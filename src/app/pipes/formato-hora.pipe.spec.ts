import { FormatoHoraPipe } from './formato-hora.pipe';

describe('FormatoHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoHoraPipe();
    expect(pipe).toBeTruthy();
  });
});

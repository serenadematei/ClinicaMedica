import { FechaCustomizadaPipe } from './fecha-customizada.pipe';

describe('FechaCustomizadaPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaCustomizadaPipe();
    expect(pipe).toBeTruthy();
  });
});

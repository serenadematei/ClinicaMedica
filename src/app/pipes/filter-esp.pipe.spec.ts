import { FilterEspPipe } from './filter-esp.pipe';

describe('FilterEspPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEspPipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterPacPipe } from './filter-pac.pipe';

describe('FilterPacPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPacPipe();
    expect(pipe).toBeTruthy();
  });
});
